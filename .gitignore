# Raw dataset - too large for GitHub (493MB, exceeds 100MB limit) and easily
# re-downloaded from Kaggle (thoughtvector/customer-support-on-twitter) per README.
data/twcs.csv
data/sample.csv

# Derived data files - regenerated by data_prep.py, no need to version them.
data/corpus.parquet
data/test_sample.parquet

# Python
__pycache__/
*.pyc
*.pyo

# OS/editor cruft
.DS_Store
.vscode/

# Keep these even though they're inside data/ - they're the actual deliverables:
# data/golden_set.csv, data/golden_set.xlsx  (hand-labelled golden set)
# outputs/*.jsonl, outputs/*.csv             (agent/baseline results, judge scores, summary metrics)
